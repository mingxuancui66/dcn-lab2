apiVersion: v1
kind: Service
metadata:
  name: sample-time-app
  labels:
    app: sample-time-app
spec:
  type: NodePort
  selector:
    app: sample-time-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
